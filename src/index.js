import React from 'react';
import ReactDOM from 'react-dom';

import App from './App';

/*everything inside the root node will be managed by React*/
ReactDOM.render(<App />, document.getElementById('root'));
