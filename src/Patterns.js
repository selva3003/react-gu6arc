/* 
HOC
  1. We go for higher order components when we need the common functinality that needs to be shared across the componets. 
  2. its basically a function that takes a component as an argument and returns a new component with extra functionalities. 
Render Props
  
  
*/
